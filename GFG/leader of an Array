#include<iostream>
#include<bits/stdc++.h>
void leadersofArray(int [], int );
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t)
    {
        int n;
        cin>>n;
        int a[n];
        for(int i = 0; i < n; i++)
        {
            cin>>a[i];
        }
        leadersofArray(a,n);
        t--;
        cout<<"\n";
    }
    return 0;
}
void leadersofArray(int a[], int n)
{
    for(int i = 0; i < n; i++)
    {
        int count = 0;
        for(int j = i; j < n; j++)
        {
            if(a[i] > a[j])
            {
                count++;
                if(count == n-1-i)
                {
                    cout<<a[i]<<" ";
                }
            }
        }
    }
    cout<<a[n-1];
}
